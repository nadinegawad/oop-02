namespace OOP_AS_03
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region Q01
            //Define a struct "Person" with properties "Name" and "Age". 

            //Person person = new Person(3);
            //person.AddPerson(0, "Nadine", 30);
            //person.AddPerson(1, "Zein", 40);
            //person.AddPerson(2, "Mohamed", 20);

            //person.DisplayData();

            #endregion

            #region Q02
            //Write a C# program that takes details of 3 persons as input from the user and displays the name and age of the oldest person.
            //Person person02 = new Person(3);
            //Console.WriteLine("Enter 3 person names and their ages : ");
            //person02.AddPerson(0,Console.ReadLine() ,int.Parse(Console.ReadLine()));
            //person02.AddPerson(1, Console.ReadLine(), int.Parse(Console.ReadLine()));
            //person02.AddPerson(2, Console.ReadLine(), int.Parse(Console.ReadLine()));

            //person02.DisplayData();
            //person02.GetOldestPerson();
            #endregion


            #region Q03
            //1.	Design and implement a Class for the employees in a company:

            #endregion 
        }
        }
}
